<%= form_with(model: chatbox, local: true) do |form| %>
  <% if chatbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chatbox.errors.count, "error") %> prohibited this chatbox from being saved:</h2>

      <ul>
      <% chatbox.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
